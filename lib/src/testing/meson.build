# ---- Include directory for local live/fake test support ----
testing_inc = include_directories('.')

# FakeExchange is header-only and intentionally outside production exchange adapters.
testing_sources = files(
    'fake_exchange.h'
)
