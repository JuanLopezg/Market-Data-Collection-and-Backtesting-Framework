# ---- Include directories ----
filter_inc = include_directories('.')

# ---- Source files (only translation units, not headers) ----
filter_sources = files(
    'market_filter.cpp',
    'benchmark_above_sma_filter.cpp'
)
