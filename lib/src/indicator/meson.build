# ---- Include directories ----
indicator_inc = include_directories('.')

# ---- Source files (only translation units, not headers) ----
indicator_sources = files(
    'indicator_spec.cpp',
    'indicator_engine.cpp',
    'indicator_calculators.cpp'
)
