# ---- Include directory for this folder ----
sizing_inc = include_directories('.')

# ---- Header-only portfolio sizing policies ----
sizing_sources = files(
    'portfolio_sizer.h',
    'portfolio_sizing_diagnostics.h',
    'equal_weight_sizer.h',
    'volatility_target_sizer.h'
)
